#include <stdio.h>
int main() {
    int numberprintf("Enter an integer: ");
    scanf("%d", &number);
    printf("Enter an integer: ");
    scanf("%d", &number);
    if (number % 2 == 0) {
        printf("%d is Even.\n", number);

